<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
    <title>Blog</title>
    <link href="https://fonts.googleapis.com/css2?family=Lato:wght@100;300;400;700;900&display=swap" rel="stylesheet">
    <link rel="stylesheet" type="text/css" href="/styles/header.css">
    <script
        src="https://code.jquery.com/jquery-3.5.1.min.js"
        integrity="sha256-9/aliU8dGd2tb6OSsuzixeV4y/faTqgFtohetphbbj0="
        crossorigin="anonymous">
    </script>
</head>
<body>
    <section class="navbar">
        <div class="nav-c">
            <a class="home fl" href="/blogs">Home</a>
            <a class="home fl" href="#">About</a>
            <% if(currentUser && currentUser.username === "abhi@gmail.com"){ %>
                <a class="new-post fl" href="/blogs/new">New Post</a>
            <% } %>
            <% if(!currentUser){ %>
            <a class="sign fr" href="/signup">Signup</a>
            <a class="login fr" href="/login">Login</a>
            <% } else { %>
            <a class="logout fr" href="/logout">Logout</a>
            <a class="user fr" href="#">Hi, <%= currentUser.firstname %></a>
            <% } %>
            <div class="search-tab">
                <form action="/blogs" method="GET">
                    <input class="search-field" type="text" name="search" placeholder="Blog search...">
                    <input class="search-btn" type="submit" value="Search">
                </form>
            </div>
        </div>
    </section>
    <% if(fail && fail.length > 0){ %>
        <div class="fail-container">
            <div class="fail"><%= fail %></div>
        </div>
    <% } %>
    <% if(success && success.length > 0){ %>
        <div class="success-container">
            <div class="success"><%= success %></div>
        </div>
    <% } %>